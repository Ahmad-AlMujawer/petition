<p>These people have already singed the petition</p>
<ul>
    {{#signers}}
        <li>
            {{#if homepage}}
                <a href={{homepage}}>{{first}}>{{last}}></a>
            {{else}}
                {{first}}
                {{last}}
            {{/if}}
            {{#if age}}
                ({{age}},{{city}})
            {{/if}}
        </li>
    {{/signers}}
</ul>